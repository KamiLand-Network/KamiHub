# PlaceholderAPI

> PlaceholderAPI（简称PAPI）在本插件中的运用

## 使用说明

在 `config.yml` 的大部分可显示文本中，以及 `messages.yml` 里的全部值，都可以使用 PlaceholderAPI 的占位符。

当这些行为由玩家触发时，插件会以该玩家的身份解析这些变量，确保占位符被正确替换为玩家的相关信息。

**备注**：预计在 1.1 ~ 1.2 版本中，会加入 KamiHub 的内置 PAPI 占位符，提供更多自定义功能。
